class User {
  String codeno, name, email, phone, password, credit, datereg, quantity;
  User({
    this.codeno,
    this.name,
    this.email,
    this.password,
    this.phone,
    this.credit,
    this.datereg,
    this.quantity,
  });
}
